{
  "name": "nome-do-bot",
  "version": "1.0.0",
  "description": "Descrição do bot",
  "main": "main.js",  // O arquivo principal que contém o código do bot
  "scripts": {
    "start": "node main.js"  // O comando para iniciar o bot
  },
  "dependencies": {
    "node-telegram-bot-api": "^0.64.0"  // A dependência para a biblioteca do Telegram Bot API
  }
}
